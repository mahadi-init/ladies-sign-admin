import PageTop from "@/components/native/PageTop";

export default function Dashboard() {
  return (
    <div>
      <PageTop
        title={"Dashboard"}
        subTitle={"Multiple data in a single place"}
      />
    </div>
  );
}
